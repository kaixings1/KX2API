/**
 * 任务拆解 + 执行器集成测试（mock 模式）
 *
 * 测试场景：
 *   1. static 模式拆解 → 验证子任务数量、依赖链
 *   2. executor 执行 → 用 mock runners 验证顺序执行 + 结果合并
 *   3. 自动重试 → 模拟失败 → 验证重试次数
 *   4. 断点续跑 → 标记部分完成 → 恢复后跳过
 *   5. 端到端 → 拆解 → 执行 → 持久化验证
 */
export {};
